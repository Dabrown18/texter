import {StyleSheet} from 'react-native';

const images = {

};

const styles = StyleSheet.create({
  titleStyle: {
    marginTop: 5,
    marginLeft: 15,
    marginRight: 15,
    fontWeight: '600',
    fontSize: 17
  }
});

export {images, styles};
